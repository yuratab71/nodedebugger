import { createTheme } from "@mui/material/styles";
// import { PALLETTE } from "../main/constants/pallette";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#44B864",
        },
        secondary: {
            main: "#151B35",
        },
    },
});
